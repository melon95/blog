<a name="eHQg5"></a>
# 计算属性和侦听属性
用户自定义的计算属性和侦听属性会在 `Vue` 组件初始化的过程中初始化：
```javascript
 // src/core/instance/state.js
 function initState (vm: Component) {
   vm._watchers = []
   const opts = vm.$options
   if (opts.props) initProps(vm, opts.props)
   if (opts.methods) initMethods(vm, opts.methods)
   if (opts.data) {
     initData(vm)
   } else {
     observe(vm._data = {}, true /* asRootData */)
   }
   // 计算属性初始化
   if (opts.computed) initComputed(vm, opts.computed)
   if (opts.watch && opts.watch !== nativeWatch) {
     // 初始化watch
     initWatch(vm, opts.watch)
   }
 }
```
<a name="EOVOs"></a>
## Computed(计算属性)
**用途：用于处理一个或多个值的变化，并返回变化后的结果。关注点在于值的变化。**<br />`computed` 初始化：
```javascript
 function initComputed (vm: Component, computed: Object) {
   // _computedWatchers用来保存计算属性的Watcher实例
   const watchers = vm._computedWatchers = Object.create(null)
   // computed properties are just getters during SSR
   // 判断是不是SSR(服务器渲染)
   const isSSR = isServerRendering()
   // 遍历定义的computed对象
   for (const key in computed) {
     const userDef = computed[key]
     const getter = typeof userDef === 'function' ? userDef : userDef.get
 
     if (!isSSR) {
       // create internal watcher for the computed property.
       // 计算属性在创建watcher实例的时候不会调用get方法来获取
       // 只有当要用到计算属性的时候才会求值
       // 在求值的时候当前的computedWatcher会被收集到所有用到的data的dep中
       // 当其中一个变化的时候计算属性也会变化
       watchers[key] = new Watcher(
         vm,
         getter || noop,
         noop,
         { lazy: true },
       )
     }
 
     // component-defined computed properties are already defined on the
     // component prototype. We only need to define computed properties defined
     // at instantiation here.
     // 判断key有没有在vm上存在
     if (!(key in vm)) {
       defineComputed(vm, key, userDef)
     } else if (process.env.NODE_ENV !== 'production') {
       if (key in vm.$data) {
         warn(`The computed property "${key}" is already defined in data.`, vm)
       } else if (vm.$options.props && key in vm.$options.props) {
         warn(`The computed property "${key}" is already defined as a prop.`, vm)
       }
     }
   }
 }
```
定义计算属性：
```javascript
 function defineComputed (
   target: any,
   key: string,
   userDef: Object | Function
 ) {
   // 只讨论非服务器渲染的情况，所以为true
   const shouldCache = !isServerRendering()
   // 如果我们定义的计算属性是一个函数
   // 大部分情况下都是这样
   if (typeof userDef === 'function') {
     // 读取计算属性的值得时候会触发这个getter
     sharedPropertyDefinition.get = shouldCache
       ? createComputedGetter(key)
       : createGetterInvoker(userDef)
     sharedPropertyDefinition.set = noop
   } else {
     // 如果定义的计算属性是一个对象
     sharedPropertyDefinition.get = userDef.get
       ? shouldCache && userDef.cache !== false
         ? createComputedGetter(key)
         : createGetterInvoker(userDef.get)
       : noop
     sharedPropertyDefinition.set = userDef.set || noop
   }
   // 可以通过this.加计算属性的命名就能访问计算属性
   // 同时也设置了计算属性的属性描述对象
   Object.defineProperty(target, key, sharedPropertyDefinition)
 }
```
创建计算属性的求值函数：
```javascript
 function createComputedGetter (key) {
   return function computedGetter () {
     const watcher = this._computedWatchers && this._computedWatchers[key]
     if (watcher) {
       // 因为是计算属性所以dirty为true
       if (watcher.dirty) {
         // 求值
         watcher.evaluate()
       }
       if (Dep.target) {
         // 收集依赖
         watcher.depend()
       }
       return watcher.value
     }
   }
 }
```
<a name="461oS"></a>
## Watch(侦听属性)
**用途：用于观测一个值的变化，并进行后续的处理。关注点在于后续处理。**<br />`Watch` 初始化：
```javascript
 function initWatch (vm: Component, watch: Object) {
   for (const key in watch) {
     const handler = watch[key]
     // 大部分情况下handler都是一个对象或者函数
     if (Array.isArray(handler)) {
       for (let i = 0; i < handler.length; i++) {
         createWatcher(vm, key, handler[i])
       }
     } else {
       createWatcher(vm, key, handler)
     }
   }
 }
```
规范化属性：
```javascript
 function createWatcher (
   vm: Component,
   expOrFn: string | Function,
   handler: any,
   options?: Object
 ) {
   // 规范化handler
   // 判断handler是不是对象
   if (isPlainObject(handler)) {
     options = handler
     handler = handler.handler
   }
   if (typeof handler === 'string') {
     handler = vm[handler]
   }
   return vm.$watch(expOrFn, handler, options)
 }
```
创建`Watcher` :
```javascript
 Vue.prototype.$watch = function (
     expOrFn: string | Function,
     cb: any,
     options?: Object
   ): Function {
     const vm: Component = this
     // 规范化格式
     if (isPlainObject(cb)) {
       return createWatcher(vm, expOrFn, cb, options)
     }
     options = options || {}
     // 设置user为true表示是用户watcher
     options.user = true
     // expOrFn可以是一个函数或者是以点来分割的字符串比如：obj.a
     const watcher = new Watcher(vm, expOrFn, cb, options)
     // immediate为true则立即调用一次
     if (options.immediate) {
     cb.call(vm, watcher.value)
     // 返回一个取消watch的方法
     return function unwatchFn () {
       watcher.teardown()
     }
   }
```
