当谈到继承时，JavaScript 只有一种结构：对象。每个实例对象（ object ）都有一个私有属性（称之为 __proto__ ）指向它的构造函数的原型对象（prototype ）。该原型对象也有一个自己的原型对象( __proto__ ) ，层层向上直到一个对象的原型对象为 `null`。根据定义，`null` 没有原型，并作为这个原型链中的最后一个环节。<br />JavaScript 对象是动态的属性“包”（指其自己的属性）。JavaScript 对象有一个指向一个原型对象的链。当试图访问一个对象的属性时，它不仅仅在该对象上搜寻，还会搜寻该对象的原型，以及该对象的原型的原型，依次层层向上搜索，直到找到一个名字匹配的属性或到达原型链的末尾。
> 被构造函数创建的实例对象的 `[[Prototype]]` 指向构造函数的的 `prototype` 属性。

<a name="S6iBD"></a>
## 1. 原型链继承


```javascript
function Prototype(msg) {
  this.msg = msg
}
Prototype.prototype.outputMsg = function() {
  console.log(this.msg)
}

function Instance() {

}
// 这行代码不能写Instance函数内部，这里也不用纠正Instance.prototype.constructor的指向
// 因为Instance.prototype没有这个属性
// 当执行new Prototype()的时候没有办法灵活的传递参数
Instance.prototype = new Prototype()

const instance = new Instance()
console.log(instance)

instance instanceof Instance // true
instance instanceof Prototype // true
```
![image.png](https://cdn.nlark.com/yuque/0/2020/png/299895/1586501033570-a8b74dac-9ce1-4c36-a63f-b331e803ff14.png#align=left&display=inline&height=129&margin=%5Bobject%20Object%5D&name=image.png&originHeight=129&originWidth=690&size=11655&status=done&style=none&width=690)<br />缺点：

- 没办法灵活的传递参数
- 多个实例共同引用同一个原型



<a name="h7ivn"></a>
## 2. 借用构造函数+原型链继承


```javascript
function Prototype(msg) {
  this.msg = msg
}
Prototype.prototype.outputMsg = function() {
  console.log(this.msg)
}

function Instance(msg) {
  // 让Instance实例实现Prototype上面的实例方法
  Prototype.call(this, msg)
}
// 让Instance继承Prototype.prototype上面的原型方法和属性
Instance.prototype = new Prototype()

const instance = new Instance('instance')
console.log(instance)

instance instanceof Instance // true
instance instanceof Prototype // true
```

<br />![image.png](https://cdn.nlark.com/yuque/0/2020/png/299895/1586501208686-e1c9f997-4c37-417f-8d53-7cf8db1702be.png#align=left&display=inline&height=144&margin=%5Bobject%20Object%5D&name=image.png&originHeight=144&originWidth=672&size=13820&status=done&style=none&width=672)<br />缺点：

- Instance的原型和实例上会存在同名属性



<a name="Ho9q7"></a>
## 3. 借用构造函数+Object.create()


```javascript
function Prototype(msg) {
  this.msg = msg
}
Prototype.prototype.outputMsg = function() {
  console.log(this.msg)
}

function Instance(msg) {
  Prototype.call(this, msg)
}、
// 这里不能直接令Instance.prototype = Prototype.prototype
// 如果这样做的话往Instance.prototype上添加属性，也会改变Prototype.prototype
Instance.prototype = Object.create(Prototype.prototype)

const instance = new Instance('instance')
console.log(instance)

instance instanceof Instance // true
instance instanceof Prototype // true
```
![image.png](https://cdn.nlark.com/yuque/0/2020/png/299895/1586501444723-16225385-94fb-4fa9-b7bc-fc563f96bd08.png#align=left&display=inline&height=132&margin=%5Bobject%20Object%5D&name=image.png&originHeight=132&originWidth=706&size=11970&status=done&style=none&width=706)<br />

<a name="ZSoBI"></a>
## 4. Class继承


```javascript
class Prototype {
  constructor(msg) {
    this.msg = msg
  }
  output() {
    console.log(this.msg)
  }
}
class Instance extends Prototype {
  constructor(msg) {
    super(msg)
  }
}
const instance = new Instance('instance')
console.log(instance)


instance instanceof Instance // true
instance instanceof Prototype // true
```

<br />![image.png](https://cdn.nlark.com/yuque/0/2020/png/299895/1586502294615-acfab772-67f9-436a-84a0-6cc2fb2cac71.png#align=left&display=inline&height=139&margin=%5Bobject%20Object%5D&name=image.png&originHeight=139&originWidth=581&size=12633&status=done&style=none&width=581)<br />**注意**：这里的 `Instance.prototype` 比第三种中方法中的 `Instance.prototype` 多了一个 `constructor` 属性。
<a name="2HXio"></a>
## 总结
虽然上面罗列了四种方法，但是在实际使用中还是推荐使用第四种或者第三种，前面两种只是作为一个介绍的过度。
